apiVersion: v1
kind: Service
metadata:
  name: mcp-bunkerweb
  namespace: bunkerweb
  labels:
    app: mcp-bunkerweb
    app.kubernetes.io/name: mcp-bunkerweb
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app: mcp-bunkerweb
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
